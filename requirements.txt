# Wajib: tidak ada dependency eksternal untuk fitur inti (terminal + txt + json)
# hanya menggunakan Python standard library.

# Opsional: hanya dibutuhkan jika mau export laporan ke PDF (flag --pdf)
fpdf2>=2.7.0

# Opsional: khusus Windows, supaya warna terminal (GOOD/WARNING/BAD) tampil
# benar di Command Prompt lama. Tidak wajib di Linux/macOS atau di Windows
# Terminal/PowerShell modern (sudah native support ANSI).
colorama>=0.4.6; sys_platform == "win32"
